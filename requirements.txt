# Agente de impresión térmica GestionCom
# Instalar con: pip install -r requirements.txt

# Comunicación con el servidor
requests==2.32.3

# Impresión ESC/POS (impresoras térmicas)
python-escpos==3.1

# Portal web local
flask==3.1.0

# Renderizado TrueType + barcode en imagen PIL
Pillow>=10.0.0
python-barcode>=0.15.1

# Servicio Windows (solo en Windows)
pywin32>=308; sys_platform == "win32"
